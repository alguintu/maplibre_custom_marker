# maplibre_custom_marker

Render a Flutter widget to PNG and register it as a style image in **maplibre_gl**, then place it as a `Symbol`.

Platforms follow `maplibre_gl`: **Android**, **iOS**, **Web**.

## Example
See `/example`.

## License
MIT
